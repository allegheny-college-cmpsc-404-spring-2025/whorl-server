global:
  scrape_interval: 60s
remote_write:
  - url: <given by grafana>
    basic_auth:
      username: <Will be generated by grafana>
      password: <Token given by grafana>
scrape_configs:
  - job_name: node
    static_configs:
      - targets: ["localhost:8000"]